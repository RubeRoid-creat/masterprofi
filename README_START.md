# 🚀 MasterProfi - Быстрый старт

## ✅ Проект готов к запуску!

Все файлы созданы, зависимости установлены. Остался только один шаг.

## 📝 Что нужно сделать

### Создайте базу данных через pgAdmin

1. **Откройте pgAdmin** (поиск в Windows)
2. **Подключитесь** к PostgreSQL серверу (пароль: `postgres` или ваш)
3. **Правой кнопкой** на "Databases" → Create → Database
4. **Имя:** `masterprofi`
5. **Save**

Подробно: см. `STEP_BY_STEP_DB_SETUP.md`

### Создайте .env файл

В папке `backend` создайте файл `.env`:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=masterprofi

JWT_SECRET=dev-secret-key-123456789
JWT_EXPIRES_IN=1h

PORT=3000
NODE_ENV=development
```

### Запустите сервер

```bash
cd backend
npm run start:dev
```

Откройте: http://localhost:3000/api/docs

---

**Готово! Проект запущен!** 🎉

