# 🔧 Исправление проблем с подключением к базе данных

## Проблема:
- Backend возвращает 500 ошибки
- Метрики показывают 0
- Ошибка "Ошибка в Англии" (исправлено на фронтенде)

## Решение:

### 1. ✅ Создан .env файл
- Файл `.env` создан из `env.example`
- Настройки БД по умолчанию:
  - DB_HOST=localhost
  - DB_PORT=5432
  - DB_USERNAME=postgres
  - DB_PASSWORD=your_password_here (НУЖНО ИЗМЕНИТЬ!)
  - DB_NAME=masterprofi

### 2. ⚠️ Что нужно сделать:

#### A. Настроить PostgreSQL:
1. Убедитесь, что PostgreSQL установлен и запущен
2. Создайте базу данных:
   ```sql
   CREATE DATABASE masterprofi;
   ```
3. Создайте пользователя (если нужно):
   ```sql
   CREATE USER masterprofi WITH PASSWORD 'masterprofi_pass';
   GRANT ALL PRIVILEGES ON DATABASE masterprofi TO masterprofi;
   ```

#### B. Обновить .env файл:
Откройте `backend/.env` и измените:
```
DB_USERNAME=postgres  # или ваш пользователь
DB_PASSWORD=ваш_пароль  # замените your_password_here
DB_NAME=masterprofi
```

#### C. Запустить миграции:
```bash
cd backend
npm run migration:run
```

#### D. Перезапустить backend:
После настройки БД перезапустите backend, чтобы подключиться к базе данных.

### 3. ✅ Проверка подключения:
После настройки проверьте:
- Backend запускается без ошибок подключения к БД
- API `/api/stats/dashboard` возвращает данные
- Метрики в админ-панели загружаются

### 4. 🔍 Логи:
Если проблемы остаются, проверьте логи бэкенда в терминале - там будут видны конкретные ошибки подключения к БД.





