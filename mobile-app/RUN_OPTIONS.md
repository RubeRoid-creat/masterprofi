# 🚀 Варианты запуска приложения

## 📱 Вариант 1: Веб-версия (Рекомендуется для разработки)

### Запуск:
```powershell
cd "Z:\App RBT\mobile-app"
npx expo start --web
```

Откроется в браузере автоматически.

---

## 📱 Вариант 2: Android эмулятор

### Требования:
1. **Android Studio** установлен
2. **Android SDK** настроен
3. **AVD (Android Virtual Device)** создан

### Создание эмулятора:

1. Откройте **Android Studio**
2. Перейдите: **Tools → Device Manager** (или **Tools → AVD Manager**)
3. Нажмите **Create Device**
4. Выберите устройство (например, **Pixel 5**)
5. Выберите системный образ (рекомендуется **API 33** или выше)
6. Нажмите **Finish**

### Запуск эмулятора:

**Вариант A: Из Android Studio**
- В **Device Manager** нажмите ▶️ (Play) рядом с вашим эмулятором

**Вариант B: Из командной строки**
```powershell
# Найдите путь к emulator.exe (обычно в Android SDK)
$env:ANDROID_HOME\emulator\emulator.exe -avd <имя_эмулятора>

# Или если ANDROID_HOME не установлен:
"C:\Users\<YourUser>\AppData\Local\Android\Sdk\emulator\emulator.exe" -list-avds
```

### После запуска эмулятора:
```powershell
cd "Z:\App RBT\mobile-app"
npx expo start
# Нажмите 'a' для запуска на Android
```

---

## 📱 Вариант 3: Физическое Android устройство

### Настройка устройства:

1. **Включите режим разработчика:**
   - Откройте **Настройки** → **О телефоне**
   - Нажмите 7 раз на **Номер сборки**

2. **Включите отладку по USB:**
   - **Настройки** → **Для разработчиков** → **Отладка по USB** ✓

3. **Подключите устройство:**
   - Подключите телефон через USB
   - Разрешите отладку на телефоне (появится запрос)

4. **Проверьте подключение:**
```powershell
adb devices
```

Должно показать ваше устройство:
```
List of devices attached
ABC123XYZ    device
```

5. **Запустите приложение:**
```powershell
cd "Z:\App RBT\mobile-app"
npx expo start
# Нажмите 'a' для запуска на Android
```

---

## 📱 Вариант 4: iOS Simulator (только на macOS)

```powershell
cd "Z:\App RBT\mobile-app"
npx expo start
# Нажмите 'i' для запуска на iOS
```

---

## 🔧 Проверка установки Android SDK

### Проверка переменных окружения:

```powershell
# Проверить ANDROID_HOME
echo $env:ANDROID_HOME

# Если пусто, установите:
$env:ANDROID_HOME = "C:\Users\$env:USERNAME\AppData\Local\Android\Sdk"
[System.Environment]::SetEnvironmentVariable('ANDROID_HOME', $env:ANDROID_HOME, 'User')

# Добавить в PATH
$env:PATH += ";$env:ANDROID_HOME\platform-tools;$env:ANDROID_HOME\emulator"
```

---

## ⚡ Быстрый старт (без Android)

**Рекомендуется для начала разработки:**

```powershell
cd "Z:\App RBT\mobile-app"
npx expo start --web
```

Откроется в браузере на `http://localhost:8081` или подобном порту.

---

## 🐛 Устранение проблем

### Проблема: "No Android connected device found"

**Решение 1: Запустите в веб-режиме**
```powershell
npx expo start --web
```

**Решение 2: Создайте эмулятор**
- Следуйте инструкциям в **Вариант 2** выше

**Решение 3: Проверьте adb**
```powershell
adb devices
# Если команда не найдена, установите Android SDK Platform Tools
```

---

## 📝 Примечания

- **Веб-версия** отлично подходит для разработки UI и логики
- **Эмулятор** нужен для тестирования нативных функций (камера, геолокация и т.д.)
- **Физическое устройство** - лучший вариант для финального тестирования








