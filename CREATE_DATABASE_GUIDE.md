# 📊 Создание базы данных MasterProfi

## Вариант 1: Через pgAdmin (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте pgAdmin

1. Нажмите **Win** + **S** (поиск Windows)
2. Введите "pgAdmin"
3. Откройте **pgAdmin 4**

### Шаг 2: Подключитесь к серверу PostgreSQL

1. В левом дереве найдите **Servers** → **PostgreSQL 14** (или другой номер версии)
2. Щелкните по серверу
3. Введите пароль, который вы установили при установке PostgreSQL
   - Если не помните пароль, обычно это **postgres** или пароль, который вы установили

### Шаг 3: Создайте базу данных

1. Щелкните правой кнопкой мыши по **Databases**
2. Выберите **Create** → **Database...**
3. В появившемся окне:
   - **Database name:** `masterprofi`
   - Оставьте остальные настройки по умолчанию
   - Нажмите **Save**

### Готово! ✅

База данных создана!

---

## Вариант 2: Через SQL Query

### Шаг 1: Откройте Query Tool

1. В pgAdmin щелкните правой кнопкой по серверу **PostgreSQL**
2. Выберите **Query Tool**

### Шаг 2: Выполните SQL команды

Скопируйте и вставьте в окно Query Tool:

```sql
-- Создать базу данных
CREATE DATABASE masterprofi;

-- Создать пользователя (опционально)
CREATE USER masterprofi WITH PASSWORD 'masterprofi_pass';

-- Выдать права
GRANT ALL PRIVILEGES ON DATABASE masterprofi TO masterprofi;
```

### Шаг 3: Запустите запрос

Нажмите кнопку **Execute** (▶) или **F5**

---

## Вариант 3: Через командную строку (psql)

### Если у вас установлен psql в PATH:

```bash
# Войдите в PostgreSQL
psql -U postgres

# Выполните SQL команды
CREATE DATABASE masterprofi;
\q
```

### Если psql не в PATH:

Найдите путь к psql в установке PostgreSQL, обычно это:
```
C:\Program Files\PostgreSQL\14\bin\psql.exe
C:\Program Files\PostgreSQL\15\bin\psql.exe
C:\Program Files\PostgreSQL\16\bin\psql.exe
```

Затем выполните:

```bash
# Замените 14 на вашу версию
"C:\Program Files\PostgreSQL\14\bin\psql.exe" -U postgres
CREATE DATABASE masterprofi;
\q
```

---

## Вариант 4: Использовать существующую базу данных

Если у вас уже есть база данных и вы хотите переиспользовать её:

1. Найдите имя существующей базы в pgAdmin
2. Обновите `backend/.env` файл:
   ```
   DB_NAME=ваша_база_данных
   ```

---

## ⚠️ Важные моменты

### Пароль PostgreSQL

Если вы забыли пароль или не знаете:
1. Откройте **Services** (Win + R, введите `services.msc`)
2. Найдите **postgresql-x64-14** (или другую версию)
3. Остановите службу
4. Отредактируйте файл `pg_hba.conf` (обычно `C:\Program Files\PostgreSQL\14\data\pg_hba.conf`)
5. Измените `scram-sha-256` на `trust` для локальных подключений
6. Перезапустите PostgreSQL
7. Войдите как postgres без пароля
8. Верните настройки обратно

### Проверка создания базы данных

В pgAdmin:
1. Разверните **Databases** в дереве слева
2. Вы должны увидеть базу данных **masterprofi**

Через SQL:
```sql
SELECT datname FROM pg_database;
```

Вы увидите список всех баз данных, включая `masterprofi`

---

## 🎯 После создания базы данных

### Следующие шаги:

1. ✅ **База данных создана** (вы здесь)
2. ⏳ **Создайте .env файл** в папке `backend`
3. ⏳ **Запустите сервер**: `cd backend && npm run start:dev`

---

## 📝 Настройка .env файла

После создания базы данных, создайте файл `backend/.env`:

```env
# Если создали пользователя masterprofi
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=masterprofi
DB_PASSWORD=masterprofi_pass
DB_NAME=masterprofi

# ИЛИ если используете существующего пользователя postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=ваш_пароль_postgres
DB_NAME=masterprofi

# JWT
JWT_SECRET=dev-secret-key-123456789
JWT_EXPIRES_IN=1h

# Server
PORT=3000
NODE_ENV=development
```

---

## 🆘 Проблемы?

### "PostgreSQL server is not running"

**Решение:**
```powershell
# Проверить статус
Get-Service PostgreSQL

# Запустить если не запущен
Start-Service PostgreSQL
```

### "Authentication failed"

**Решение:** Проверьте пароль в `.env` файле

### "Database already exists"

**Решение:** Ничего страшного! Просто используйте существующую базу данных.

---

**Готово! База данных создана.** 🎉

Следующий шаг: Запустите backend сервер!

