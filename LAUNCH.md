# 🚀 Запуск MasterProfi

## Проект готов! Остался один шаг

Проект **MasterProfi** полностью настроен. Для запуска нужно только создать базу данных.

## ⚡ Быстрый запуск (3 шага)

### 1️⃣ Откройте pgAdmin или любой PostgreSQL клиент

Найдите ярлык **pgAdmin** в меню Пуск или подключитесь через командную строку.

### 2️⃣ Создайте базу данных

Выполните SQL команды:

```sql
CREATE DATABASE masterprofi;
CREATE USER masterprofi WITH PASSWORD 'masterprofi_pass';
GRANT ALL PRIVILEGES ON DATABASE masterprofi TO masterprofi;
\q
```

**Внимание:** Если используете пользователя `postgres`, используйте:
```sql
CREATE DATABASE masterprofi;
\q
```

### 3️⃣ Запустите сервер

Откройте новое окно терминала:

```bash
cd "Z:\App RBT\backend"
npm run start:dev
```

## ✅ Проверка

После запуска откройте в браузере:
- **API**: http://localhost:3000/api
- **Swagger**: http://localhost:3000/api/docs

Вы должны увидеть JSON ответ с `status: "OK"`

## 🧪 Тестирование

### Вариант 1: Через Swagger UI (рекомендуется)

1. Откройте http://localhost:3000/api/docs
2. Нажмите на `/api/auth/register` → Try it out
3. Введите данные:
```json
{
  "email": "test@example.com",
  "password": "password123",
  "firstName": "Test",
  "lastName": "User"
}
```
4. Нажмите Execute

### Вариант 2: Через curl

```bash
# Регистрация
curl -X POST http://localhost:3000/api/auth/register ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"test@example.com\",\"password\":\"password123\",\"firstName\":\"Test\",\"lastName\":\"User\"}"

# Вход (получите token)
curl -X POST http://localhost:3000/api/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"test@example.com\",\"password\":\"password123\"}"
```

## 🎯 Следующие шаги

После успешного запуска:

1. ✅ Протестируйте API через Swagger
2. ✅ Создайте несколько тестовых пользователей
3. ✅ Проверьте работу Orders
4. ⏳ Начните разработку новых функций

## 🐛 Если что-то не работает

### Сервер не запускается

**Ошибка:** "Can't connect to PostgreSQL"
- Проверьте, запущен ли PostgreSQL
- Убедитесь, что база данных создана
- Проверьте пароль в команде SQL

**Решение:**
```sql
-- Проверьте подключение
psql -U postgres

-- Список баз данных
\l

-- Если базы masterprofi нет, создайте
CREATE DATABASE masterprofi;
```

### Ошибка миграций

**Если видите ошибки** `relation "users" does not exist`:

TypeORM автоматически создаст таблицы при запуске (synchronize: true в development).

### Порт занят

**Ошибка:** `Port 3000 is already in use`

Измените порт в backend/.env:
```
PORT=3001
```

## 📞 Поддержка

- **Документация**: См. docs/SETUP.md
- **Структура проекта**: См. PROJECT_SUMMARY.md
- **Swagger**: http://localhost:3000/api/docs

---

**Удачи в разработке! 🚀**

