# ✅ Исправлена ошибка добавления рефералов!

## 🐛 Проблема:
Internal Server Error при добавлении реферала

## 🔍 Причина:
Конфликт имен колонок в базе данных:
- Таблица `users` использовала `referrer_id`
- Таблица `referrals` также использовала `referrer_id`
- TypeORM не мог правильно создать связи

## ✅ Решение:
Изменено имя колонки в entity User:
- Было: `referrer_id`
- Стало: `invited_by_id`

Теперь структура:
- `users.invited_by_id` - кто пригласил пользователя
- `referrals.referrer_id` - кто пригласил в реферале
- `referrals.referred_id` - кого пригласили

---

**Перезапустите backend сервер для применения исправления!**

```powershell
# Остановите серверы
Get-Process node | Stop-Process -Force

# Запустите снова
.\START_SERVERS.ps1
```

