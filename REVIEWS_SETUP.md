# ⭐ Система отзывов и рейтингов

## ✅ Реализованный функционал

### Backend
- ✅ `Review` entity с полями: orderId, masterId, clientId, rating (1-5), comment
- ✅ `ReviewsService` с автоматическим пересчетом рейтинга мастера
- ✅ REST API endpoints для CRUD операций с отзывами
- ✅ Статистика по отзывам для мастеров и общая статистика
- ✅ Валидация: отзыв можно оставить только на завершенный заказ
- ✅ Один отзыв на заказ (уникальный индекс)

### Frontend
- ✅ Компонент `StarRating` - 5-звездочный рейтинг (интерактивный и readonly)
- ✅ Компонент `ReviewForm` - форма создания/редактирования отзыва
- ✅ Компонент `ReviewsList` - список отзывов с фильтрацией
- ✅ Интеграция в страницу заказов:
  - Кнопка "Отзыв" для завершенных заказов
  - Отображение существующего рейтинга
  - Модальное окно для создания/редактирования отзыва

## 📊 API Endpoints

### Создать отзыв
```
POST /api/reviews
Body: {
  orderId: string,
  masterId: string,
  rating: number (1-5),
  comment?: string
}
```

### Получить все отзывы
```
GET /api/reviews?masterId=...&orderId=...
```

### Получить отзыв по заказу
```
GET /api/reviews/order/:orderId
```

### Получить статистику мастера
```
GET /api/reviews/master/:masterId/stats
Response: {
  total: number,
  average: number,
  distribution: {
    5: number,
    4: number,
    3: number,
    2: number,
    1: number
  }
}
```

### Получить общую статистику
```
GET /api/reviews/overall/stats
Response: {
  totalReviews: number,
  averageRating: number,
  distribution: {...},
  mastersWithReviews: number
}
```

### Обновить отзыв
```
PATCH /api/reviews/:id
Body: {
  rating?: number,
  comment?: string,
  isPublished?: boolean
}
```

### Удалить отзыв
```
DELETE /api/reviews/:id
```

## 🎯 Как использовать

### Оставить отзыв на заказ

1. Перейдите на страницу **Заказы**
2. Найдите завершенный заказ (статус "Завершен")
3. Нажмите кнопку **⭐ Отзыв** рядом с заказом
4. Выберите рейтинг (1-5 звезд)
5. Оставьте комментарий (опционально)
6. Нажмите **"Отправить отзыв"**

### Редактировать отзыв

1. Найдите заказ, на который уже оставлен отзыв
2. Рядом с заказом будет отображаться текущий рейтинг
3. Нажмите **"Редактировать"**
4. Измените рейтинг и/или комментарий
5. Сохраните изменения

### Просмотреть отзывы мастера

Используйте компонент `ReviewsList`:
```tsx
<ReviewsList masterId="master-uuid" limit={10} />
```

## 📈 Автоматический пересчет рейтинга

При создании, обновлении или удалении отзыва система автоматически:
1. Пересчитывает средний рейтинг мастера
2. Обновляет `rating` в `MasterProfile`
3. Обновляет `reviewsCount` в `MasterProfile`

Формула расчета:
```
rating = (sum of all ratings) / (number of reviews)
```

## 🗄️ База данных

### Таблица `reviews`:
- `id` (uuid, primary key)
- `order_id` (uuid, foreign key → orders)
- `master_id` (uuid, foreign key → users)
- `client_id` (uuid, foreign key → users)
- `rating` (int, 1-5)
- `comment` (text, nullable)
- `is_published` (boolean, default: true)
- `created_at` (timestamp)
- `updated_at` (timestamp)
- **Уникальный индекс**: (master_id, order_id) - один отзыв на заказ

### Обновление MasterProfile:
После каждого изменения отзывов автоматически обновляются:
- `rating` (decimal 3,2) - средний рейтинг
- `reviewsCount` (int) - количество отзывов

## ⚠️ Валидация и правила

1. **Отзыв можно оставить только на завершенный заказ** (`status === "completed"`)
2. **Клиент может оставить отзыв только на свой заказ**
3. **Один отзыв на заказ** - если отзыв уже существует, он обновляется
4. **Рейтинг обязателен** (1-5 звезд)
5. **Комментарий опционален**

## 🎨 Компоненты

### StarRating
- Интерактивный режим: выбор рейтинга
- Readonly режим: только отображение
- Размеры: sm, md, lg
- Опция показа числового значения

### ReviewForm
- Валидация режима
- Автозаполнение для редактирования
- Обработка ошибок
- Состояние загрузки

### ReviewsList
- Фильтрация по мастеру или заказу
- Сортировка по дате (новые сначала)
- Отображение информации о клиенте
- Опциональная информация о заказе

## 🚀 Следующие шаги

Возможные улучшения:
- [ ] Модерация отзывов (публикация/скрытие)
- [ ] Ответы мастеров на отзывы
- [ ] Полезность отзывов (лайки/дизлайки)
- [ ] Фильтрация и сортировка отзывов
- [ ] Экспорт отзывов в отчеты
- [ ] Уведомления мастерам о новых отзывах

