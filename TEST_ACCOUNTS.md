# 🔐 Тестовые учетные данные приложения

## 📋 Учетные данные для входа:

### 👨‍💼 Администратор:
- **Email:** `admin@masterprofi.ru`
- **Пароль:** `admin123`
- **Роль:** ADMIN
- **Реферальный код:** ADMIN001

### 🔧 Мастер:
- **Email:** `master@masterprofi.ru`
- **Пароль:** `master123`
- **Роль:** MASTER
- **Телефон:** +79991234567
- **Реферальный код:** MASTER001
- **Реферер:** Администратор

### 👤 Клиент:
- **Email:** `client@masterprofi.ru`
- **Пароль:** `client123`
- **Роль:** CLIENT
- **Телефон:** +79991234568
- **Реферальный код:** CLIENT001
- **Реферер:** Мастер

## 🚀 Как создать эти аккаунты на сервере:

### Вариант 1: Через seed скрипт (рекомендуется)

На сервере выполните:

```bash
cd /opt/masterprofi

# Убедитесь что backend запущен
docker compose -f docker-compose.prod.yml ps

# Запустите seed скрипт
docker compose -f docker-compose.prod.yml exec backend npm run seed:run
```

### Вариант 2: Вручную через API

```bash
# Создать администратора
curl -X POST http://212.74.227.208:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@masterprofi.ru",
    "password": "admin123",
    "role": "admin",
    "firstName": "Администратор",
    "lastName": "Системы"
  }'

# Создать мастера
curl -X POST http://212.74.227.208:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "master@masterprofi.ru",
    "password": "master123",
    "role": "master",
    "firstName": "Иван",
    "lastName": "Мастеров",
    "phone": "+79991234567"
  }'

# Создать клиента
curl -X POST http://212.74.227.208:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "client@masterprofi.ru",
    "password": "client123",
    "role": "client",
    "firstName": "Петр",
    "lastName": "Клиентов",
    "phone": "+79991234568"
  }'
```

### Вариант 3: Через базу данных (если API не работает)

```bash
docker compose -f docker-compose.prod.yml exec postgres psql -U masterprofi -d masterprofi
```

Затем в psql выполните (пароли нужно захешировать через bcrypt):

```sql
-- Внимание: Пароли нужно захешировать через bcrypt!
-- Используйте онлайн генератор или Node.js:
-- const bcrypt = require('bcrypt');
-- bcrypt.hash('admin123', 10).then(hash => console.log(hash));

-- Пример (замените на реальный bcrypt hash):
INSERT INTO users (id, email, "passwordHash", role, "firstName", "lastName", "isActive", "emailVerified", "referralCode", "createdAt", "updatedAt")
VALUES (
  uuid_generate_v4(),
  'admin@masterprofi.ru',
  '$2b$10$YourBcryptHashHere', -- Замените на реальный hash
  'admin',
  'Администратор',
  'Системы',
  true,
  true,
  'ADMIN001',
  now(),
  now()
);
```

## 🔍 Проверка существующих пользователей:

```bash
docker compose -f docker-compose.prod.yml exec postgres psql -U masterprofi -d masterprofi -c "SELECT email, role, \"isActive\" FROM users;"
```

## ⚠️ Важно:

1. **Безопасность:** После создания тестовых аккаунтов на production сервере, **обязательно смените пароли** на более сложные!

2. **Seed скрипт:** Скрипт проверяет существование пользователей и не создает дубликаты. Если пользователь уже существует, он будет пропущен.

3. **Пароли:** Все пароли в seed скрипте хешируются через bcrypt с солью 10.

## 📱 Использование в мобильном приложении:

Вы можете использовать эти же учетные данные для входа в мобильное приложение:

- **Администратор:** `admin@masterprofi.ru` / `admin123`
- **Мастер:** `master@masterprofi.ru` / `master123`
- **Клиент:** `client@masterprofi.ru` / `client123`

## 🔄 После создания аккаунтов:

1. Войдите в web-admin: http://212.74.227.208:8080
2. Используйте учетные данные администратора
3. Проверьте работу всех разделов
4. Протестируйте мобильное приложение с учетными данными мастера

