# 🚀 Кэширование с Redis

## ✅ Реализованный функционал

### Backend
- ✅ **CacheModule** - глобальный модуль для работы с Redis
- ✅ **CacheService** - сервис для управления кэшем
- ✅ **StatsModule** - модуль статистики с кэшированием
- ✅ Кэширование статистики Dashboard (TTL: 1 минута)
- ✅ Кэширование MLM статистики (TTL: 5 минут)
- ✅ Кэширование статистики отзывов (TTL: 5 минут)
- ✅ Автоматическая инвалидация кэша при изменениях
- ✅ Fallback на in-memory cache если Redis недоступен

## 📦 Установка Redis

### Windows (через Docker)
```bash
docker run -d -p 6379:6379 --name redis redis:latest
```

### Windows (через Chocolatey)
```bash
choco install redis-64
redis-server
```

### Linux/Mac
```bash
# Ubuntu/Debian
sudo apt-get install redis-server

# Mac
brew install redis
brew services start redis
```

## ⚙️ Конфигурация

Добавьте в `.env` файл:

```env
# Redis настройки
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379

# Или используйте URL напрямую
# REDIS_URL=redis://:password@host:port

# Отключить Redis (будет использоваться in-memory cache)
USE_REDIS=false
```

## 🎯 Использование

### CacheService API

```typescript
import { CacheService, CacheKeyPrefix, CacheTTL } from "./cache/cache.service";

// Получить значение
const value = await cacheService.get<string>("key");

// Установить значение
await cacheService.set("key", "value", CacheTTL.SHORT);

// Получить или установить (кэш-попадание или мисс)
const data = await cacheService.getOrSet(
  "key",
  async () => {
    // Выполнить тяжелую операцию
    return await expensiveOperation();
  },
  CacheTTL.MEDIUM
);

// Удалить значение
await cacheService.del("key");

// Инвалидировать кэш модуля
await cacheService.invalidateModule(CacheKeyPrefix.DASHBOARD_STATS);
await cacheService.invalidateStats();
await cacheService.invalidateMlm();
```

### TTL (Time To Live)

```typescript
enum CacheTTL {
  SHORT = 60,        // 1 минута
  MEDIUM = 300,      // 5 минут
  LONG = 1800,       // 30 минут
  VERY_LONG = 3600,  // 1 час
}
```

### Префиксы ключей

```typescript
enum CacheKeyPrefix {
  DASHBOARD_STATS = "dashboard:stats",
  MLM_STRUCTURE = "mlm:structure",
  MLM_COMMISSIONS = "mlm:commissions",
  REPORTS = "reports",
  USERS_LIST = "users:list",
  ORDERS_LIST = "orders:list",
  MASTER_PROFILES = "masters:profiles",
  REVIEWS_STATS = "reviews:stats",
  SCHEDULE_SLOTS = "schedule:slots",
}
```

## 📊 Кэшируемые данные

### Dashboard Statistics
- **Endpoint**: `GET /api/stats/dashboard`
- **TTL**: 1 минута
- **Кэш-ключ**: `dashboard:stats:all`
- **Данные**: 
  - Количество пользователей (всего, мастеров, клиентов)
  - Количество заказов (всего, в ожидании, завершенных)
  - Количество платежей и общий доход
  - Статистика отзывов

### MLM Statistics
- **Endpoint**: `GET /api/mlm/stats/:userId`
- **TTL**: 5 минут
- **Кэш-ключ**: `mlm:structure:${userId}`
- **Инвалидация**: При создании реферала, бонуса, выплаты

### MLM Overall Stats
- **Endpoint**: `GET /api/mlm/overall`
- **TTL**: 5 минут
- **Кэш-ключ**: `mlm:commissions:overall`

### Reviews Statistics
- **Endpoint**: `GET /api/reviews/master/:masterId/stats`
- **TTL**: 5 минут
- **Кэш-ключ**: `reviews:stats:master:${masterId}`
- **Инвалидация**: При создании/обновлении/удалении отзыва

### Reviews Overall Stats
- **Endpoint**: `GET /api/reviews/overall/stats`
- **TTL**: 5 минут
- **Кэш-ключ**: `reviews:stats:overall`

## 🔄 Автоматическая инвалидация

Кэш автоматически инвалидируется при:

1. **Создании/обновлении отзыва**:
   - Инвалидируется кэш статистики мастера
   - Инвалидируется общая статистика отзывов
   - Инвалидируется статистика dashboard

2. **Изменениях MLM** (можно добавить):
   - При создании реферала
   - При расчете комиссии
   - При выплате бонуса

3. **Изменениях заказов/пользователей** (можно добавить):
   - При создании/обновлении заказа
   - При создании/обновлении пользователя

## 📈 Производительность

- **Без кэша**: ~200-500ms для статистики dashboard
- **С кэшем**: ~5-20ms (в 10-50 раз быстрее!)
- **Redis**: Поддержка кластеризации для высокой доступности
- **Fallback**: In-memory cache если Redis недоступен

## 🧪 Тестирование

### Проверка подключения к Redis
```bash
# В консоли Redis
redis-cli
> PING
PONG
```

### Проверка кэша
```bash
# Посмотреть все ключи
redis-cli KEYS "*"

# Получить значение
redis-cli GET "dashboard:stats:all"

# Проверить TTL
redis-cli TTL "dashboard:stats:all"
```

## ⚠️ Важные замечания

1. **Redis не обязателен**: Если Redis недоступен, система автоматически переключится на in-memory cache
2. **TTL**: Настройте TTL в зависимости от требований к актуальности данных
3. **Инвалидация**: Важно инвалидировать кэш при изменениях данных
4. **Memory**: Redis использует память, следите за размером кэша
5. **Production**: Используйте Redis в production для лучшей производительности

## 🚀 Следующие шаги

Возможные улучшения:
- [ ] Кэширование списка пользователей с пагинацией
- [ ] Кэширование списка заказов с фильтрами
- [ ] Кэширование профилей мастеров
- [ ] Кэширование слотов расписания
- [ ] Distributed cache для multi-instance deployment
- [ ] Cache warming (предзагрузка данных)
- [ ] Метрики кэш-попаданий и миссов

