# MLM Tree Node Component

Переиспользуемый компонент узла дерева MLM сети с полной информацией о члене команды.

## Props

### Required Props
- `member: MLMMember` - Объект члена MLM сети
- `depth: number` - Глубина узла в дереве (0 для корня)
- `isExpanded: boolean` - Раскрыт ли узел
- `onToggle: () => void` - Обработчик раскрытия/сворачивания

### Optional Props
- `onPress?: (member: MLMMember) => void` - Обработчик нажатия на узел
- `onContact?: (member: MLMMember, type: 'call' | 'message') => void` - Обработчик контакта
- `showChildren?: boolean` - Показывать ли детей (по умолчанию true)
- `childrenCount?: number` - Количество детей (если не загружены)

## Особенности

✅ **User Avatar and Name** - Аватар и имя пользователя с цветом ранга  
✅ **Rank and Level** - Ранг и уровень в сети  
✅ **Team Size** - Размер команды (totalDownline)  
✅ **Monthly Earnings** - Месячный заработок с форматированием  
✅ **Performance Indicators** - Индикаторы производительности  
✅ **Expand/Collapse** - Функциональность раскрытия/сворачивания  
✅ **Recruitment Status** - Статус рекрутинга  
✅ **Quick Contact Actions** - Быстрые действия для звонка и сообщения  
✅ **Accessibility** - Полная поддержка доступности  

## Использование

### Базовое использование

```tsx
import { MLMTreeNode } from './src/components/mlm/MLMTreeNode';
import { MLMMember } from './src/types/mlm';

const member: MLMMember = {
  id: '1',
  name: 'John Doe',
  level: 1,
  rank: 'Gold',
  status: 'active',
  // ... другие поля
};

<MLMTreeNode
  member={member}
  depth={0}
  isExpanded={false}
  onToggle={() => setIsExpanded(!isExpanded)}
  onPress={(member) => navigateToMember(member.id)}
/>
```

### С действиями контакта

```tsx
<MLMTreeNode
  member={member}
  depth={0}
  isExpanded={true}
  onToggle={() => {}}
  onPress={(member) => {}}
  onContact={(member, type) => {
    if (type === 'call') {
      callMember(member.phone);
    } else {
      sendMessage(member.id);
    }
  }}
/>
```

## Визуальная структура

```
┌─────────────────────────────────────────┐
│ [▶] [Avatar] Name [Gold Badge]  $18,000 │
│      L1 ● active                        │
│      ● Excellent Performance            │
├─────────────────────────────────────────┤
│ 👥 45  ➕ 8  💰 800K  [Recruiting]      │
├─────────────────────────────────────────┤
│ Joined: Jan 2024  [📞 Call] [💬 Message]│
└─────────────────────────────────────────┘
```

## Ранги и цвета

### Ранги:
- **Bronze** - Янтарный (amber-600)
- **Silver** - Серый (gray-400)
- **Gold** - Желтый (yellow-500)
- **Platinum** - Синий (blue-400)
- **Diamond** - Фиолетовый (purple-500)

### Статусы:
- **Active** - Зеленый индикатор
- **Inactive** - Серый индикатор
- **Pending** - Желтый индикатор

## Индикаторы производительности

Рассчитывается на основе соотношения личных продаж к продажам команды:

- **Excellent** (≥50%) - Зеленый
- **Good** (30-49%) - Синий
- **Average** (15-29%) - Желтый
- **Low** (<15%) - Красный

## Статус рекрутинга

Отображается, если:
- Статус члена = 'active'
- Количество прямых рекрутов > 0

Показывает зеленый бейдж "Recruiting".

## Быстрые действия

### Call Button
- Зеленая кнопка с иконкой телефона
- Открывает звонок через Linking или вызывает onContact

### Message Button
- Синяя кнопка с иконкой сообщения
- Открывает SMS через Linking или вызывает onContact

## Доступность

### Accessibility Labels:
- Полное описание узла для screen readers
- Информация о ранге, уровне, размере команды
- Метки для всех интерактивных элементов

### Accessibility Hints:
- Подсказки для действий
- Описание раскрытия/сворачивания

## Интеграция с NetworkTree

Компонент используется внутри `NetworkTree` для отображения иерархии:

```tsx
import { NetworkTree } from './src/components/mlm/NetworkTree';

<NetworkTree
  rootMember={rootMember}
  maxDepth={5}
  onMemberPress={(member) => {}}
  onMemberContact={(member, type) => {}}
/>
```

## Производительность

- Оптимизированное отображение без лишних ре-рендеров
- Эффективная обработка глубоких вложенностей
- Ленивая загрузка данных детей








