# 📊 Пошаговая инструкция: Создание базы данных

## 🎯 Самый простой способ (через pgAdmin GUI)

### Шаг 1: Откройте pgAdmin

1. Нажмите клавишу **Windows**
2. Введите: **pgAdmin**
3. Нажмите Enter

Должно открыться окно pgAdmin.

### Шаг 2: Войдите в PostgreSQL

1. В левой панели найдите **Servers**
2. Разверните **PostgreSQL 14** (или другую версию)
3. Щелкните по серверу
4. **Введите пароль** когда попросят
   - Обычно это: `postgres`
   - Или пароль, который вы устанавливали при установке

### Шаг 3: Создайте базу данных

1. **Правой кнопкой** по **Databases** (слева)
2. Выберите **Create** → **Database...**
3. В поле **Database name:** введите `masterprofi`
4. **Оставьте все остальные поля по умолчанию**
5. Нажмите **Save** (или **Enter**)

### ✅ Готово!

Теперь в списке Databases должна быть база данных **masterprofi**

---

## 🎯 Через SQL (если нет GUI)

### Шаг 1: Откройте Query Tool

В pgAdmin:
1. Щелкните **правой кнопкой** по серверу **PostgreSQL**
2. Выберите **Query Tool**

### Шаг 2: Скопируйте SQL код

Скопируйте этот код:

```sql
CREATE DATABASE masterprofi;
```

### Шаг 3: Выполните

1. Вставьте код в окно Query Tool
2. Нажмите кнопку **Execute** (▶) вверху
3. Или нажмите **F5**

### ✅ Готово!

Вы должны увидеть сообщение: `Query returned successfully`

---

## 🔍 Проверка

После создания, проверьте что база есть:

1. В pgAdmin слева в дереве разверните **Databases**
2. Вы должны увидеть **masterprofi** в списке

---

## ⚙️ Настройка для запуска проекта

После создания базы данных, нужно создать файл `.env`:

### Создание .env файла

Откройте файл: `backend/env.example`

Скопируйте его содержимое в новый файл: `backend/.env`

Или создайте файл вручную с таким содержимым:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=masterprofi

JWT_SECRET=dev-secret-key-123456
JWT_EXPIRES_IN=1h
PORT=3000
NODE_ENV=development
```

**Внимание:** Замените `postgres` и `postgres` на ваши учетные данные PostgreSQL!

---

## 🚀 Запуск проекта

После создания базы данных и файла .env:

```bash
cd backend
npm run start:dev
```

Сервер запустится на http://localhost:3000

---

## 📸 Визуальная инструкция

```
pgAdmin окно:
┌─────────────────────────────────┐
│ Object Browser                  │
├─────────────────────────────────┤
│ ▼ Servers                       │
│   ▼ PostgreSQL 14              │
│     ▼ Databases       ← ПКМ    │
│       masterprofi     ← НОВЫЙ! │
│     ▼ Login/Group Roles        │
│     ▼ Tablespaces              │
└─────────────────────────────────┘
```

---

## 🐛 Частые проблемы

### "Connection refused"

PostgreSQL не запущен. Запустите:
```powershell
Start-Service PostgreSQL
```

### "Password authentication failed"

Неверный пароль. Попробуйте стандартный `postgres`

### "Database already exists"

База уже создана! Пропустите этот шаг.

---

**Готово! Теперь можно запускать проект!** 🎉

