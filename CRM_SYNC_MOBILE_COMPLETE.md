# CRM Синхронизация - Mobile App Реализация ✅

## ✅ Реализовано

### 1. API Клиент (RTK Query)
- ✅ `crmApi.ts` - полный набор endpoints для синхронизации
- ✅ Типизированные интерфейсы для всех сущностей
- ✅ Автоматическая авторизация через JWT

### 2. Локальное хранилище (WatermelonDB)
- ✅ Модели данных:
  - `CrmContact` - контакты
  - `CrmDeal` - сделки
  - `CrmTask` - задачи
  - `CrmSyncQueue` - очередь синхронизации
- ✅ Обновлена схема базы данных (version 2)
- ✅ Миграция для новых таблиц
- ✅ Зарегистрированы модели в database.ts

### 3. Сервис синхронизации
- ✅ `crmSyncService.ts` - основной сервис синхронизации
- ✅ Первоначальная синхронизация
- ✅ Инкрементальная синхронизация
- ✅ Очередь исходящих изменений
- ✅ Автоматическая синхронизация (опционально)
- ✅ Обработка офлайн режима

### 4. UI Компоненты
- ✅ `CrmSyncScreen.tsx` - экран синхронизации
- ✅ Отображение статуса синхронизации
- ✅ Индикатор подключения к сети
- ✅ Кнопки для ручной синхронизации
- ✅ Отображение ошибок

## 📁 Структура файлов

```
mobile-app/src/
├── store/api/
│   └── crmApi.ts                    # RTK Query API клиент
├── database/
│   ├── schema.ts                    # Обновлена схема (version 2)
│   ├── migrations/index.ts          # Миграция для CRM таблиц
│   ├── models/
│   │   ├── CrmContact.ts
│   │   ├── CrmDeal.ts
│   │   ├── CrmTask.ts
│   │   └── CrmSyncQueue.ts
│   └── database.ts                  # Зарегистрированы новые модели
├── services/
│   └── crmSyncService.ts            # Сервис синхронизации
└── screens/
    └── CrmSyncScreen.tsx             # UI экран синхронизации
```

## 🔌 API Endpoints (используются через RTK Query)

### Hooks
- `useInitialSyncQuery` - первоначальная синхронизация
- `useIncrementalSyncQuery` - инкрементальная синхронизация
- `useOutgoingSyncMutation` - отправка изменений
- `useGetSyncStatusQuery` - статус синхронизации
- `useCreateConnectionMutation` - создать подключение
- `useGetConnectionQuery` - получить подключение
- `useGetAuthUrlQuery` - получить OAuth URL
- `useDeleteConnectionMutation` - удалить подключение

## 🚀 Использование

### Инициализация синхронизации

```typescript
import { crmSyncService } from '../services/crmSyncService';

// Первоначальная синхронизация
await crmSyncService.initialSync('amocrm');

// Инкрементальная синхронизация
await crmSyncService.incrementalSync();

// Автоматическая синхронизация каждые 5 минут
crmSyncService.startAutoSync(5 * 60 * 1000);
```

### Добавление изменения в очередь

```typescript
// Создать/обновить контакт
await crmSyncService.queueChange(
  'contact',
  'contact-id',
  'CREATE',
  {
    name: 'John Doe',
    phone: '+1234567890',
    email: 'john@example.com',
  }
);
```

### Работа с локальными данными

```typescript
import { database } from '../database/database';
import CrmContact from '../database/models/CrmContact';
import { Q } from '@nozbe/watermelondb';

// Получить все контакты
const contacts = await database.collections
  .get<CrmContact>('crm_contacts')
  .query()
  .fetch();

// Найти контакт по CRM ID
const contact = await database.collections
  .get<CrmContact>('crm_contacts')
  .query(Q.where('crm_id', '123'))
  .fetch();
```

## 🔄 Синхронизация

### Процесс синхронизации

1. **Первоначальная синхронизация:**
   - Загружает все данные из CRM
   - Сохраняет в локальную базу данных
   - Помечает как синхронизированные

2. **Инкрементальная синхронизация:**
   - Загружает только изменения с последней синхронизации
   - Обновляет существующие записи или создает новые
   - Обрабатывает конфликты (last write wins)

3. **Очередь исходящих изменений:**
   - Изменения добавляются в очередь при офлайн режиме
   - Автоматически отправляются при подключении к интернету
   - Retry логика при ошибках

## 📱 UI Экран

Экран `CrmSyncScreen` предоставляет:
- Статус подключения к сети
- Статус синхронизации (последняя синхронизация, количество записей)
- Кнопки для ручной синхронизации
- Отображение ошибок

## 🔐 Безопасность

- JWT токены автоматически добавляются в запросы
- Локальные данные хранятся в зашифрованной базе данных
- Синхронизация только при наличии активного подключения к CRM

## ⚙️ Конфигурация

API URL настраивается через `config/environments.ts`:
- Development: `http://localhost:3000/api` (iOS/Web) или `http://10.0.2.2:3000/api` (Android)
- Production: настраивается через переменные окружения

## 📊 Следующие шаги

1. ✅ Backend интеграция - **ГОТОВО**
2. ✅ Mobile App - **ГОТОВО**
3. ⏳ Интеграция в основное приложение:
   - Добавить навигацию к экрану синхронизации
   - Добавить экраны для просмотра контактов, сделок, задач
   - Интегрировать создание/редактирование CRM данных

## 🧪 Тестирование

1. Запустить backend сервер
2. Запустить мобильное приложение
3. Перейти на экран синхронизации
4. Подключиться к CRM (через OAuth)
5. Выполнить первоначальную синхронизацию
6. Проверить локальные данные в базе данных





