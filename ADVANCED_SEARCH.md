# 🔍 Расширенный поиск с автодополнением

## ✅ Реализованная функциональность

### 1. **Поиск по всем полям**

- ✅ Универсальный поиск по всем указанным полям
- ✅ Поддержка множественных полей одновременно
- ✅ Поиск по email, имени, фамилии, роли и другим полям

### 2. **Автодополнение**

- ✅ Автоматические предложения на основе данных
- ✅ История поисков в автодополнении
- ✅ Появление после 2+ символов
- ✅ Навигация клавиатурой (Enter, Escape)

### 3. **История поисков**

- ✅ Автоматическое сохранение в localStorage
- ✅ Хранение до 10 последних запросов
- ✅ Быстрый доступ из автодополнения
- ✅ Отдельное хранилище для каждой страницы

### 4. **Расширенные фильтры**

- ✅ Фильтрация по любым полям
- ✅ Операторы: содержит, равно, начинается с, заканчивается на, больше, меньше
- ✅ Комбинирование нескольких фильтров
- ✅ Визуальное отображение активных фильтров

### 5. **Сохраненные фильтры**

- ✅ Сохранение комбинаций фильтров с названием
- ✅ Быстрая загрузка сохраненных фильтров
- ✅ Управление сохраненными фильтрами (удаление)
- ✅ Хранение в localStorage

## 📁 Структура

```
web-admin/src/
├── hooks/
│   └── useAdvancedSearch.ts     # Хук для расширенного поиска
├── components/
│   └── AdvancedSearch.tsx       # Компонент поиска с UI
└── pages/
    └── Users.tsx                 # Интеграция (пример)
```

## 🔧 Использование

### Базовое использование:

```typescript
import { useAdvancedSearch } from "../hooks/useAdvancedSearch";
import { AdvancedSearch } from "../components/AdvancedSearch";

function MyPage() {
  const data = [...]; // Ваши данные
  
  const {
    searchQuery,
    filters,
    filteredData,
    autocompleteSuggestions,
    showAutocomplete,
    searchHistory,
    savedFilters,
    handleSearchChange,
    handleSearchSubmit,
    setShowAutocomplete,
    addFilter,
    removeFilter,
    clearFilters,
    saveFilter,
    loadFilter,
    deleteFilter,
  } = useAdvancedSearch({
    data,
    searchFields: ["email", "name", "description"], // Поля для поиска
    storageKey: "my_page_search", // Ключ для localStorage
    debounceMs: 300, // Опционально
  });

  const availableFields = [
    { value: "email", label: "Email", type: "text" },
    { value: "name", label: "Название", type: "text" },
    { value: "amount", label: "Сумма", type: "number" },
  ];

  return (
    <AdvancedSearch
      searchQuery={searchQuery}
      onSearchChange={handleSearchChange}
      onSearchSubmit={handleSearchSubmit}
      autocompleteSuggestions={autocompleteSuggestions}
      showAutocomplete={showAutocomplete}
      onShowAutocompleteChange={setShowAutocomplete}
      searchHistory={searchHistory}
      filters={filters}
      onAddFilter={addFilter}
      onRemoveFilter={removeFilter}
      onClearFilters={clearFilters}
      savedFilters={savedFilters}
      onSaveFilter={saveFilter}
      onLoadFilter={loadFilter}
      onDeleteFilter={deleteFilter}
      availableFields={availableFields}
    />
  );
}
```

## 🎯 Операторы фильтров

1. **contains** (Содержит) - для текстового поиска
2. **equals** (Равно) - точное совпадение
3. **startsWith** (Начинается с) - начало строки
4. **endsWith** (Заканчивается на) - конец строки
5. **greaterThan** (Больше) - для чисел
6. **lessThan** (Меньше) - для чисел

## 💾 Хранение данных

Данные хранятся в `localStorage` с ключами:
- `{storageKey}_history` - история поисков
- `{storageKey}_savedFilters` - сохраненные фильтры

## 📊 Примеры использования

### Поиск по email:

Просто начните вводить email в строке поиска.

### Фильтр по роли:

1. Нажмите "Фильтры"
2. Выберите поле "Роль"
3. Выберите оператор "Равно"
4. Введите "admin"
5. Нажмите "Добавить"

### Сохранение фильтра:

1. Создайте нужные фильтры
2. Нажмите "Сохраненные"
3. Введите название (например, "Только админы")
4. Нажмите "Сохранить"

### Загрузка фильтра:

1. Нажмите "Сохраненные"
2. Выберите нужный фильтр
3. Нажмите "Загрузить"

## 🎨 Кастомизация

### Изменение полей поиска:

```typescript
const { filteredData } = useAdvancedSearch({
  data: users,
  searchFields: ["email", "firstName", "lastName", "phone"], // Добавьте нужные поля
  storageKey: "users_search",
});
```

### Изменение операторов фильтров:

Отредактируйте `availableFields` для добавления поддержки разных типов:

```typescript
const availableFields = [
  { value: "amount", label: "Сумма", type: "number" }, // Для чисел
  { value: "date", label: "Дата", type: "date" },     // Для дат
];
```

## 🚀 Интеграция на других страницах

### Для Orders:

```typescript
const {
  searchQuery,
  filters,
  filteredData,
  // ... остальное
} = useAdvancedSearch({
  data: orders,
  searchFields: ["id", "clientId", "status", "description"],
  storageKey: "orders_search",
});

const availableFields = [
  { value: "status", label: "Статус", type: "text" },
  { value: "totalAmount", label: "Сумма", type: "number" },
  // ...
];
```

### Для Payments:

```typescript
const {
  searchQuery,
  filteredData,
  // ...
} = useAdvancedSearch({
  data: payments,
  searchFields: ["id", "userId", "status", "transactionId"],
  storageKey: "payments_search",
});
```

## 🔍 Особенности

1. **Производительность**: Использует `useMemo` для оптимизации
2. **Локальное хранилище**: Данные сохраняются в браузере
3. **Типобезопасность**: Полная поддержка TypeScript
4. **Расширяемость**: Легко добавить новые операторы и поля

## ✅ Готово к использованию!

Расширенный поиск полностью интегрирован в страницу Users и готов к использованию. Можно легко добавить на другие страницы (Orders, Payments) по аналогии.

