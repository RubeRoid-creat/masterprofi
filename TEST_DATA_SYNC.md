# 🧪 Тестирование обмена данными

## Быстрая проверка

### 1. Убедитесь, что Backend запущен
```bash
cd backend
npm run start:dev
# Должен быть доступен на http://localhost:3000
```

### 2. Проверьте API документацию
Откройте в браузере: http://localhost:3000/api/docs

### 3. Тест создания заказа через Admin Panel

#### В админ-панели:
1. Откройте http://localhost:5173
2. Войдите в систему
3. Создайте новый заказ через интерфейс

#### Проверка в Mobile App:
1. Откройте мобильное приложение
2. Обновите список заказов (pull-to-refresh)
3. Новый заказ должен появиться в списке

### 4. Тест принятия заказа через Mobile App

#### В мобильном приложении:
1. Откройте список заказов
2. Нажмите "Accept" на любом заказе

#### Проверка в Admin Panel:
1. Откройте админ-панель
2. Обновите страницу заказов
3. Статус заказа должен измениться на "assigned"

### 5. Тест отправки сообщения

#### В мобильном приложении:
1. Откройте детали заказа
2. Откройте чат
3. Отправьте сообщение

#### Проверка в Admin Panel:
1. Откройте заказ в админ-панели
2. Проверьте историю чата
3. Новое сообщение должно быть видно

## Тестирование через API напрямую

### 1. Получить токен
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"emailOrPhone":"test@example.com","password":"123456"}'
```

Сохраните `token` из ответа.

### 2. Создать заказ (Admin)
```bash
curl -X POST http://localhost:3000/api/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "clientId": "client-123",
    "applianceType": "washing_machine",
    "description": "Test order from admin",
    "location": {
      "address": "Test Address",
      "latitude": 55.7558,
      "longitude": 37.6173
    },
    "price": {
      "amount": 2500,
      "currency": "RUB"
    }
  }'
```

### 3. Получить заказы (Mobile App)
```bash
curl -X GET http://localhost:3000/api/orders \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Новый заказ должен быть в списке.

### 4. Принять заказ (Mobile App)
```bash
curl -X POST http://localhost:3000/api/orders/ORDER_ID/accept \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"
```

### 5. Проверить статус (Admin)
```bash
curl -X GET http://localhost:3000/api/orders/ORDER_ID \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Статус должен быть "assigned".

## Проверка WebSocket синхронизации

### 1. Откройте консоль браузера (Admin Panel)
Должны видеть подключение к WebSocket:
```
[WebSocket] Connected to ws://localhost:3000
```

### 2. В мобильном приложении
Откройте чат заказа и отправьте сообщение

### 3. В админ-панели
Сообщение должно появиться в реальном времени без обновления страницы

## Отладка проблем

### Проблема: CORS ошибки
**Решение**: Проверьте `backend/src/main.ts` - ваш origin должен быть в `allowedOrigins`

### Проблема: 401 Unauthorized
**Решение**: 
- Проверьте, что токен отправляется в заголовке `Authorization: Bearer <token>`
- Проверьте, что токен не истек
- Попробуйте перелогиниться

### Проблема: Данные не синхронизируются
**Решение**:
- Проверьте, что backend запущен
- Проверьте WebSocket соединение
- Проверьте логи в консоли браузера и терминале backend

### Проблема: Заказы не видны в Mobile App
**Решение**:
- Проверьте фильтры (возможно, заказ в другом статусе)
- Проверьте, что пользователь имеет правильную роль (master)
- Проверьте, что заказ создан с правильным статусом








