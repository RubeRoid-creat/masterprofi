# Как запустить приложение

## ✅ Быстрый запуск

### Вариант 1: Использование npx (рекомендуется)

```powershell
cd "Z:\App RBT\mobile-app"
npx expo start
```

### Вариант 2: После исправления package.json

После того как вы исправили `package.json` (скрипты обновлены), можно использовать:

```powershell
npm start
```

## 🚀 Что делать после запуска

Когда Expo Dev Server запустится, вы увидите QR-код и меню. Выберите:

- **Нажмите `a`** - для запуска на Android эмуляторе/устройстве
- **Нажмите `i`** - для запуска на iOS симуляторе (только macOS)
- **Нажмите `w`** - для запуска в браузере
- **Отсканируйте QR-код** - в приложении Expo Go на телефоне

## 📱 Установка Expo Go (для тестирования на телефоне)

1. Установите приложение:
   - [iOS App Store](https://apps.apple.com/app/expo-go/id982107779)
   - [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. Откройте Expo Go и отсканируйте QR-код из терминала

## ⚠️ Если возникают ошибки

### Ошибка: "expo не является командой"

Используйте `npx expo` вместо `expo`:

```powershell
npx expo start
npx expo install пакет
```

### Проблемы с установкой зависимостей

```powershell
# Удалите node_modules и переустановите
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item package-lock.json -ErrorAction SilentlyContinue
npm install
```

### Очистка кеша

```powershell
npx expo start --clear
```

## 🔧 Альтернативные команды

```powershell
# Запуск с очисткой кеша
npx expo start --clear

# Запуск на Android
npx expo run:android

# Запуск на iOS (macOS)
npx expo run:ios

# Запуск в браузере
npx expo start --web
```

## 📝 Примечания

- Все команды выполняются из папки `mobile-app`
- Убедитесь что Node.js 18+ установлен: `node --version`
- Если используете `npm start`, убедитесь что `package.json` обновлен
