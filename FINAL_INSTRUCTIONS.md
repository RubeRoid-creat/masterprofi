# ✅ MasterProfi - Финальные инструкции

## 🎉 Проект полностью готов!

База данных создана, зависимости установлены, код готов.

## 🚀 Запуск проекта

### Откройте НОВЫЙ терминал и выполните:

```bash
cd "Z:\App RBT\backend"
npm run start:dev
```

**ВАЖНО:** Используйте НОВЫЙ терминал/PowerShell! В текущем могут быть запущены другие процессы.

### Подождите 10-15 секунд

Вы увидите в консоли:
```
[Nest] LOG [NestFactory] Starting Nest application...
[Nest] LOG MasterProfi Backend running on: http://localhost:3000
```

### Откройте в браузере:

1. **API**: http://localhost:3000/api
   - Должно показать: `{"status":"OK","service":"MasterProfi Backend",...}`

2. **Swagger Docs**: http://localhost:3000/api/docs
   - Интерактивная документация API
   - Можете тестировать прямо в браузере!

## ✅ Что уже работает:

- ✅ PostgreSQL база данных `masterprofi` создана
- ✅ Backend сервер (NestJS) настроен
- ✅ JWT авторизация
- ✅ CRUD для Users и Orders
- ✅ Swagger документация
- ✅ Real-time через Socket.io
- ✅ MLM структура

## 🧪 Тестирование через Swagger

1. Откройте http://localhost:3000/api/docs
2. Нажмите на `/api/auth/register`
3. Нажмите "Try it out"
4. Заполните форму:
```json
{
  "email": "test@example.com",
  "password": "password123",
  "firstName": "Test",
  "lastName": "User"
}
```
5. Нажмите "Execute"
6. Скопируйте `access_token` из ответа
7. Нажмите кнопку "Authorize" (замок 🔓) вверху
8. Вставьте токен: `Bearer YOUR_TOKEN_HERE`
9. Теперь можете тестировать защищенные эндпоинты!

## 📚 Полезные ссылки

- API Endpoints: http://localhost:3000/api/docs
- Health check: http://localhost:3000/api

## 🐛 Если не запускается

### Проверьте базу данных

```bash
# Проверить подключение
"C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -h localhost -p 5433 -d masterprofi -c "SELECT version();"
```

### Проверьте .env файл

Файл `backend/.env` должен содержать:
```
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=masterprofi
```

### Убейте старые процессы

```powershell
Stop-Process -Name node -Force -ErrorAction SilentlyContinue
```

### Проверьте порт 3000

```powershell
netstat -ano | findstr :3000
```

Если порт занят, измените в `.env`:
```
PORT=3001
```

---

## 🎊 Поздравляем!

Проект **MasterProfi** готов к разработке!

Откройте Swagger UI и начните тестировать API! 🚀

